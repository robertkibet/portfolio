import styled from '@emotion/styled';

const Wrapper = styled.div`
  position: fixed;
  height: 60px;
  width: 100%;
`;
const Header =()=> (
    <Wrapper>
      <h4>Header</h4>
    </Wrapper>
  )

export default Header
